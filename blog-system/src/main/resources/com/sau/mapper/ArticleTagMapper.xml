<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sau.mapper.ArticleTagMapper">
<!--    批量新增文章标签关联-->
    <insert id="add">
        insert into article_tag(article_id, tag_id, create_time, update_time) values
        <foreach item="tag" collection="list" separator=",">
            (#{tag.articleId}, #{tag.tagId}, #{tag.createTime}, #{tag.updateTime})
        </foreach>
    </insert>
<!--    批量删除文章标签关联-->
    <delete id="delete">
        delete from article_tag where article_id in (
        <foreach item="id" collection="ids" separator=",">
            #{id}
        </foreach>)
    </delete>

</mapper>