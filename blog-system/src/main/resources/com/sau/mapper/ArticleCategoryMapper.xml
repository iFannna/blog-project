<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sau.mapper.ArticleCategoryMapper">
<!--    批量新增文章分类关联-->
    <insert id="save">
        insert into article_category(article_id, category_id, create_time, update_time) values
        <foreach item="category" collection="list" separator=",">
            (#{category.articleId}, #{category.categoryId}, #{category.createTime}, #{category.updateTime})
        </foreach>
    </insert>
<!--    根据文章ID批量删除文章分类关联-->
    <delete id="deleteByArticleIds">
        delete from article_category where article_id in (
        <foreach item="id" collection="ids" separator=",">
            #{id}
        </foreach>
        )
    </delete>
<!--    根据分类ID批量删除分类关联-->
    <delete id="deleteByCategoryIds">
        delete from article_category where category_id in (
        <foreach item="id" collection="ids" separator=",">
            #{id}
        </foreach>
        )
    </delete>

</mapper>